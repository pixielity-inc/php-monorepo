# .npmrc — php-monorepo
# pnpm configuration

# Hoist packages for Turborepo compatibility.
shamefully-hoist=true

# Don't fail on missing peer deps (PHP workspaces have no JS peers).
strict-peer-dependencies=false

auto-install-peers=true
